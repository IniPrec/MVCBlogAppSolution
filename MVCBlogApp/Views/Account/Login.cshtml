@model Core.Interfaces.DTO.LoginRequest

<div class="blog-header">
    <h1>Log In</h1>
</div>

<div class="post-list">
    @if (!ViewData.ModelState.IsValid)
    {
        <div class="text-danger">
            @Html.ValidationSummary(false)
        </div>
    }

    <form asp-controller="Account" asp-action="Login" method="post" class="post-form">
        <div class="form-group">
            <label asp-for="Email">Email</label>
            <input asp-for="Email" class="form-input" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Password">Password</label>
            <input asp-for="Password" class="form-input" type="password" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>

        <button type="submit" class="btn-publish">Login</button>
        <a asp-controller="Account" asp-action="Register" class="btn-cancel">New user? Create an account</a>
    </form>
</div>