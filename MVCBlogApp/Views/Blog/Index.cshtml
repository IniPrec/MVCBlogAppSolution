@model List<Core.Interfaces.DTO.BlogResponse>

<div class="blog-header">
    <h1>Innie's Blog</h1>
</div>

<div class="post-list">
    @foreach (var blog in Model)
    {
        <div class="post-entry">
            <div class="post-title">
                <a asp-controller="Blog" asp-action="Details" asp-route="@blog.BlogId">@blog.BlogTitle</a>
            </div>
            <div class="post-meta">@blog.UserName &middot; @blog.CreatedAt.ToString("MMM dd, yyyy")</div>
        </div>
    }
</div>